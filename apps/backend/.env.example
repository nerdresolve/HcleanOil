# Servidor
PORT=4000
NODE_ENV=development

# SMTP
# Gmail: use uma Senha de App (myaccount.google.com/apppasswords) — a senha
# normal da conta não funciona com 2FA ativa.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=contato@exemplo.com.br
SMTP_PASS=

# Remetente e destino
MAIL_FROM=contato@exemplo.com.br
MAIL_FROM_NAME=HCLEAN
MAIL_TO=contato@exemplo.com.br

# Envia e-mail de confirmação para quem preencheu o formulário
SEND_CONFIRMATION=true

# Origens liberadas no CORS (separadas por vírgula)
CORS_ORIGINS=http://localhost:3000,https://www.exemplo.com.br
